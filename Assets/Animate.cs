using UnityEngine;
using System.Collections;

public class Animate : MonoBehaviour {

	// Use this for initialization
	void Start () {
        animation["run"].speed = 2.3f;
       // animation["jump"].speed = 1.5f;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
